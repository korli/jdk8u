os_family = haiku

arch = x86

arch_model = x86_32

os_arch = haiku_x86

os_arch_model = haiku_x86_32

lib_arch = i386

compiler = sparcWorks

gnu_dis_arch = i386

sysdefs = -DHAIKU -DSPARC_WORKS -D_GNU_SOURCE -DIA32
