os_family = haiku

arch = x86

arch_model = x86_64

os_arch = haiku_x86

os_arch_model = haiku_x86_64

lib_arch = amd64

compiler = sparcWorks

gnu_dis_arch = amd64

sysdefs = -DHAIKU -DSPARC_WORKS -D_GNU_SOURCE -DAMD64
